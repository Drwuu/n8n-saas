# Client identity
CLIENT_NAME=your-client-name

# Your VPS domain/IP configuration
N8N_HOST=your-domain.com  # Replace with your actual domain
N8N_PROTOCOL=https
N8N_PORT=5678

# Database - Use strong credentials for production
POSTGRES_USER=n8n_user
POSTGRES_PASSWORD=your-strong-password-here
POSTGRES_DB=n8n_database

# Generate a unique encryption key for production
# Use: openssl rand -hex 32
N8N_ENCRYPTION_KEY=your-64-character-hex-encryption-key-here

# SSL Certificate email for Let's Encrypt
ACME_EMAIL=your-email@domain.com

# Certificate resolver (devresolver for dev, prodresolver for prod)
CERT_RESOLVER=prodresolver

# Traefik dashboard domain (optional)
TRAEFIK_HOST=traefik.your-domain.com

# Production webhook URL
WEBHOOK_URL=https://your-domain.com/webhook

# Environment settings
NODE_ENV=production
N8N_LOG_LEVEL=info

# Security settings
N8N_SECURE_COOKIE=true

# Traefik authentication (generate with: htpasswd -nb admin password)
TRAEFIK_AUTH=admin:$2y$10$example-hash-here
